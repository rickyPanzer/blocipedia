<h1><%= @wiki.title %></h1>
<p><%= @wiki.body %></p>
<%= link_to "Edit", edit_wiki_path(@wiki), class: "btn btn-success" %>
<%= link_to "Delete", wiki_path(@wiki), class: "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?'} %>